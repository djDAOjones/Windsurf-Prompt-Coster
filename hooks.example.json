{
  "_comment": "Sample Windsurf Cascade hooks config. Merge the post_cascade_response_with_transcript entry into your real ~/.codeium/windsurf/hooks.json. Replace ABSOLUTE_PATH_TO_NODE with your absolute node path (find it with 'command -v node', e.g. /opt/homebrew/bin/node) and ABSOLUTE_PATH_TO_REPO with this project's absolute path. Use an absolute node path, NOT a bare 'node': Windsurf runs hooks via 'bash -c' with a minimal GUI/launchd PATH that excludes Homebrew/nvm, so a bare 'node' would be 'command not found' and the hook would silently never fire. Tip: 'npm run setup' writes all of this for you. Confirm the exact hooks.json schema against the current docs (https://docs.windsurf.com/windsurf/cascade/hooks) — field names may differ between versions.",
  "version": 1,
  "hooks": {
    "post_cascade_response_with_transcript": [
      {
        "command": "ABSOLUTE_PATH_TO_NODE \"ABSOLUTE_PATH_TO_REPO/bin/hook.js\""
      }
    ]
  }
}
